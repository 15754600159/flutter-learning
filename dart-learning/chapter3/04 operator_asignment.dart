/**
 * 赋值运算符：= += -= *= ～/= %= ??=
 * 1. ??= 若b没有值，则赋给右边的值；若b有值，则不赋值；
 */

void main() {
  int a = 10;
  int b = 5;

  // ??= 若b没有值，则赋给右边的值；若b有值，则不赋值；
  b ??= 10;
  print(b);

  // = += -= *= ～/= %=
}